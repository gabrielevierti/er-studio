## .ehpk

`ER Studio: Pack .ehpk` runs your build, then `evenhub-cli pack`, and leaves the artifact in the project folder. Progress shows in the Console view.

Want the full layout with an editor panel and integrated terminal? `ER Studio: Open Full UI in Browser` serves it from the same local server.
